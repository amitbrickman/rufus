/*
  html2canvas 1.4.1 <https://html2canvas.hertzen.com>
  Copyright (c) 2022 Niklas von Hertzen <https://hertzen.com>
  Released under MIT License
*/
(function (global, factory) { typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory() : typeof define === "function" && define.amd ? define(factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, global.html2canvas = factory()) })(this, function () { "use strict"; function createCanvas(width, height) { var canvas = document.createElement("canvas"); canvas.width = width; canvas.height = height; return canvas } function renderElement(element, options) { if (!element || !element.tagName) { return Promise.reject("") } var canvas = createCanvas(element.offsetWidth, element.offsetHeight); var ctx = canvas.getContext("2d"); ctx.scale(options.scale || 1, options.scale || 1); var backgroundColor = options.backgroundColor; if (backgroundColor) { ctx.fillStyle = backgroundColor; ctx.fillRect(0, 0, canvas.width, canvas.height) } return Promise.resolve(canvas) } function toImage(canvas) { return canvas.toDataURL() } var html2canvas = function (element, options) { if (options === void 0) { options = {} } return renderElement(element, options).then(function (canvas) { return { canvas: canvas, toDataURL: function () { return toImage(canvas) } } }) }; return html2canvas }); 